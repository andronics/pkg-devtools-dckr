name="dckr"
description="Docker Stack Orchestrator - Multi-stack management and deployment"
version="0.1.0"
method="stow"
collection="devtools"
fold=false

# Ignore documentation files
supported_os=("linux")
depends=()
requires_commands=("curl" "docker" "docker-compose" "jq")
ignore=(
    "README.md"
)
